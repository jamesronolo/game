<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://ai.google.dev/static/site-assets/images/share-ais-513315318.png" />
</div>

# EduPlay

A React + Vite + Tailwind CSS front-end for the EduPlay interactive learning games platform.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key if you want AI question generation.
3. Start the Vite front-end:
   `npm run dev`
4. Start the Express API server separately when you need the backend routes:
   `npm run dev:server`

## Build

- `npm run build`
- `npm run preview`
